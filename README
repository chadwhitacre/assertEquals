assertEquals is an opinionated Python testing interface.


INSTALLATION

    $ pip install assertEquals


    $ make
    $ sudo make install

This installs the assertEquals Python package, the assertEquals executable, and
the assertEquals(1) man page. Please consult the Makefile to confirm and/or
change installation paths, etc.


SEE ALSO

    http://whit537.github.com/assertEquals/
